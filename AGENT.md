# 專案說明
- 這是一個在 macOS 上運行的俄羅斯方塊（Tetris）遊戲。
- 主要使用 Go 開發。
- 專案目前仍在早期階段，代理在實作前應先補齊必要的規格、結構與開發約束。

# 文件語言
- 專案文件一律使用繁體中文撰寫。
- 包含但不限於：`AGENT.md`、OpenSpec 的 `proposal.md`、`design.md`、`spec.md`、`tasks.md`、變更說明、驗證紀錄與其他專案內部文件。
- 若引用外部英文術語，可保留原文，但整體敘述、標題與條列內容仍應以繁體中文為主。

# 代理目標
- 優先產出可執行、可驗證、可持續擴充的遊戲程式，而不是只完成表面功能。
- 在不破壞既有行為的前提下，逐步補齊遊戲核心：棋盤、方塊、碰撞、旋轉、消行、分數、速度提升、結束條件、重新開始。
- 任何新增功能都應盡量保持模組化，避免把所有邏輯堆在 `main.go`。

# 技術邊界
- 語言：Go。
- 目標平台：macOS。
- 若需引入圖形、鍵盤輸入、音效或遊戲迴圈相關套件，優先選擇 Go 生態中維護良好、文件完整、社群常用的方案。
- 除非需求明確，避免引入過重依賴；能用標準庫或少量成熟套件完成，就不要堆疊框架。

# 建議結構
- `cmd/`：程式入口。
- `internal/game/`：遊戲狀態、棋盤、方塊、計分、規則。
- `internal/input/`：鍵盤輸入處理。
- `internal/render/`：畫面渲染。
- `internal/app/`：遊戲迴圈與場景協調。
- `assets/`：圖片、字型、音效等資源。
- `testdata/`：測試資料。

# 程式設計原則
- 將「遊戲規則」與「畫面/輸入」分離，核心規則應可在不啟動 UI 的情況下測試。
- 方塊旋轉、移動、落下、鎖定、消行等規則要有明確資料模型，不要依賴隱性副作用。
- 優先使用小型、單一職責的型別與函式。
- 避免過早抽象；只有在重複邏輯明確出現後才提煉介面。
- 新增邏輯時，同步補上對應測試或至少補上可驗證的手動測試步驟。

# 資料與規則要求
- 棋盤尺寸、初始速度、計分規則、等級提升規則應集中定義，避免散落硬編碼。
- Tetromino 形狀與旋轉狀態應有一致表示法，方便測試與渲染。
- 碰撞檢查必須涵蓋：
  - 邊界碰撞
  - 堆疊方塊碰撞
  - 旋轉後非法位置
  - 鎖定後的消行判定
- 隨機出塊邏輯若未明確指定，優先使用可注入隨機來源的設計，方便測試。

# 使用者體驗要求
- 至少支援：左移、右移、加速下落、旋轉、暫停/繼續、重新開始、結束畫面。
- 介面應清楚顯示目前分數、等級、下一個方塊；若實作成本合理，可加入暫存方塊（hold）。
- 輸入反應應穩定，不要因為畫面更新阻塞而漏按鍵。
- 視覺風格可保持簡潔，但需確保可辨識度與可玩性優先。

# 開發流程
- 本專案採 `openspec` 的 `spec-driven` 模式。
- 重大變更前，先確認是否需要建立或更新 proposal、spec、tasks。
- 若需求仍模糊，先收斂規格與範圍，再開始寫程式。
- 小修正可直接實作，但仍需在最終說明中清楚交代行為變更與驗證方式。

# 驗證要求
- 優先撰寫可重複執行的單元測試，特別是：
  - 方塊旋轉
  - 碰撞判定
  - 消行與計分
  - 遊戲結束判定
- 若有圖形介面，至少提供一組可執行的手動驗證流程。
- 交付前應執行格式化與測試；Go 專案預期至少通過：
  - `gofmt`
  - `go test ./...`

# 變更限制
- 不要任意改寫整個專案結構，除非目前結構已明顯阻礙後續開發。
- 不要因為追求抽象而把簡單規則包成不必要的多層介面。
- 未經需求明確化前，不要加入網路同步、排行榜、帳號系統等非核心功能。

# 交付標準
- 每次修改都應能回答三件事：
  - 改了什麼
  - 為什麼這樣改
  - 如何驗證它是對的
- 若有尚未完成的部分，要明確標示風險、缺口與下一步。
