## 1. 盤點未使用代碼

- [x] 1.1 追蹤 `cmd/tetris` 到 `internal/app`、`internal/game`、`internal/input`、`internal/render` 的呼叫鏈，列出未被生產路徑引用的函式、方法、欄位、常數與設定值候選清單
- [x] 1.2 比對候選清單與測試、文件及近期需求，標記哪些代碼應移除、哪些需要保留並說明理由

## 2. 清理與收斂模組結構

- [x] 2.1 移除 `internal/game` 中未使用的代碼、設定值或死路徑，並同步更新相關呼叫端與測試
- [x] 2.2 移除 `internal/app`、`internal/input`、`internal/render` 與 `cmd/tetris` 中未使用的代碼或多餘欄位，維持目前資料流與責任邊界清楚
- [x] 2.3 若發現低頻但必要的結構，保留並以名稱或最小註解明確表達用途，避免再次被誤判為死代碼

## 3. 驗證清理結果

- [x] 3.1 執行 `gofmt`，確認所有受影響的 Go 檔案格式一致
- [x] 3.2 執行 `go test ./...`，確認清理後專案仍可建置且核心規則測試通過
- [ ] 3.3 依手動流程檢查遊戲啟動、移動、旋轉、加速下落、暫停、重新開始與遊戲結束，確認玩家可見行為未改變
